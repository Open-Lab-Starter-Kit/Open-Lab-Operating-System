export interface ScrollerData {
  verticalPosition: number;
  verticalSize: number;
  verticalContainerSize: number;
}

export interface ConsoleTextData {
  type: string;
  text: string;
  time: string;
}
